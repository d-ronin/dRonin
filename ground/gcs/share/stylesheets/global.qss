/* Guess what. */
QMainWindow {

}

#MainContentFrame {

	background: palette(Midlight);
	padding-bottom: 5px;

}

MyTabWidget::pane > QFrame {
	/* Inside */
	background: palette(Window);
	border: 3px solid palette(Window);
}

MyTabWidget::pane {
	/* Outside */
	border: 0px;

}

/* Main tabbed content pane's tab bar */
MyTabWidget::tab-bar {

	left: 9px;

}

MyTabWidget > QTabBar::tab {

	min-height: 36px;
	max-height: 36px;

}
